# ==============================================================================
# CLOUDSCAPE NEXUS 5.0 - ENTERPRISE DEPENDENCIES
# ==============================================================================
# This file defines the strict cryptographic, network, and cloud-provider 
# SDK versions required to operate the multi-cloud graph extraction mesh.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE SECURITY & CRYPTOGRAPHY
# ------------------------------------------------------------------------------
# FIXED: Strictly defining cryptography versions to prevent the 'x509' Rust 
# binding conflict with Azure SDKs during deep environment scans.
cryptography>=42.0.5
pyOpenSSL>=24.0.0
certifi>=2024.2.2

# ------------------------------------------------------------------------------
# 2. GRAPH DATABASE & INTELLIGENCE MATRICES
# ------------------------------------------------------------------------------
# neo4j: Powers the async Bolt protocol for the Materialization Ingestor
# networkx: Powers the Heuristic Attack Path Discovery (HAPD) engine
neo4j>=5.17.0
networkx>=3.2.1

# ------------------------------------------------------------------------------
# 3. STATE MANAGEMENT & CONCURRENCY
# ------------------------------------------------------------------------------
# redis: Powers the differential state hashing and cache backends
# aiohttp: Mandatory underlying async web transport for Azure
redis>=5.0.1
aiohttp>=3.9.3
aiosignal>=1.3.1

# ------------------------------------------------------------------------------
# 4. AWS EXTRACTION FABRIC
# ------------------------------------------------------------------------------
# boto3/botocore: Bound together to ensure paginator and URM stability
boto3>=1.34.50
botocore>=1.34.50
s3transfer>=0.10.0

# ------------------------------------------------------------------------------
# 5. AZURE MANAGEMENT & DISCOVERY SDKS
# ------------------------------------------------------------------------------
# Highly specific ARM (Azure Resource Manager) libraries required for Path B
azure-core>=1.30.0
azure-identity>=1.15.0
azure-storage-blob>=12.19.0
azure-mgmt-resource>=23.0.1
azure-mgmt-compute>=30.0.0
azure-mgmt-network>=25.0.0

# ------------------------------------------------------------------------------
# 6. CONFIGURATION & SERIALIZATION
# ------------------------------------------------------------------------------
# PyYAML: Parses the tenants.yaml and settings.yaml configurations
PyYAML>=6.0.1
python-dateutil>=2.9.0